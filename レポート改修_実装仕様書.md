# レポート レイアウト改修 実装仕様書

**対象機能：** 営業所 日次レポート ／ 個人 履歴レポート  
**作成日：** 2026-06-01  
**目的：** カード型表示 → 個条書きリスト型表示への改修。KPI・増減を視覚的に即把握できるレイアウトに統一する。

---

## 1. 修正事項（今回追加の変更点）

| # | 修正内容 | 変更前 | 変更後 |
|---|---|---|---|
| 1 | 項目ラベルの色 | 青（`#2563b8`）| 黒（`#1f2430`） |
| 2 | 適用範囲 | 営業所レポートのみ | 営業所 ＋ 個人レポート 両方 |

---

## 2. 共通デザイン仕様（営業所・個人 両レポート共通）

### 2-1. カラーパレット

```css
/* テキスト */
--color-text-primary:    #1f2430;   /* 見出し・ラベル（黒。青は使わない）*/
--color-text-secondary:  #5b6270;   /* 補足テキスト */
--color-text-tertiary:   #9aa0ac;   /* 薄い注記・ゼロ値 */

/* 達成・増加（緑） */
--color-ok-text:         #1f9d61;
--color-ok-bg:           #e9f7f0;
--color-ok-border:       #79c6a3;

/* 未達・減少（赤） */
--color-warn-text:       #d0453b;
--color-warn-bg:         #fbecea;
--color-warn-border:     #e0584d;

/* 注意ステータス（アンバー） */
--color-caution-text:    #9a6a06;
--color-caution-bg:      #fbf2dc;
--color-caution-dot:     #e0a93f;

/* 構造 */
--color-surface:         #ffffff;
--color-surface-sub:     #f4f5f2;   /* サマリーバンド背景 */
--color-border:          #e3e6ea;
--color-border-light:    #f0f1f3;
```

> ⚠️ **ポイント**：項目ラベル（点検・売上・次月継続 等）は `--color-text-primary`（黒）を使う。青は使わない。

### 2-2. 共通レイアウト構造

すべてのレポートで以下の順に構成する。

```
[ヘッダー]         タイトル + 出力日 + 区切り線
[サマリーバンド]   最重要指標 2〜3 項目を横並び（背景 #f4f5f2）
[トグルバー]       期間切替 ＋ リスト/カード切替（印刷時は非表示）
[パネル群]         各ビューのコンテンツ（hidden 属性でトグル）
[フッター]         注記・出力条件（8pt グレー）
```

### 2-3. メトリクス行のレイアウト（4列グリッド）

```
[列1 項目名 18%] [列2 達成率/実績 15%] [列3 実績/目標の詳細 39%] [列4 前回比/差分 28%]
```

| 列 | 要素 | スタイル |
|---|---|---|
| 1 項目名 | 黒太字 | `color: #1f2430; font-weight: 700;` |
| 2 達成率/値 | 主役。大きく色付き | `font-size: 14pt; font-weight: 700;` 達成=緑, 未達=赤 |
| 3 詳細 | 補足（実績/目標 等） | `font-size: 9pt; color: #5b6270;` |
| 4 前回比/差分 | 増減記号 + 数値 | `font-size: 9pt; text-align: right;` 増=緑, 減=赤, なし=グレー |

**行の色分けルール：**

```css
/* 達成・増加行 */
.row-ok {
  border-left: 3px solid #79c6a3;
}

/* 未達・減少行 */
.row-warn {
  border-left: 3px solid #e0584d;
  background: #fbecea;
}

/* 比較なし行（基準日など） */
.row-base {
  border-left: 3px solid #e3e6ea;
}
```

### 2-4. ブロック（1日分 / 1週分）の構造

```html
<div class="metric-block">

  <!-- ブロックヘッダー行 -->
  <div class="block-header">
    <span class="status-dot ok">●</span>
    <span class="block-title">5/31（月）</span>
    <span class="status-badge ok">順調</span>
    <span class="block-summary">総合達成率 72%</span>  <!-- 右寄せ -->
  </div>

  <!-- メトリクス行 -->
  <div class="row row-ok">
    <span class="col-label">点検</span>
    <span class="col-rate ok">109%</span>
    <span class="col-detail">203 ／ 目標 187</span>
    <span class="col-diff up">▲ +7件 (+3.6%)</span>
  </div>

  <!-- サブ行（末見通し等。親行の下に字下げ） -->
  <div class="sub-row">
    └ 末見通し ¥11,223,603（102%）　対計画 +¥216,343
  </div>

</div>
```

### 2-5. ステータスドット・バッジ

| ステータス | ドット色 | バッジ背景 | バッジ文字色 |
|---|---|---|---|
| 順調 | `#1f9d61` | `#e9f7f0` | `#1f7d4d` |
| 注意 | `#e0a93f` | `#fbf2dc` | `#9a6a06` |
| 危険 | `#d0453b` | `#fbecea` | `#c0453a` |

### 2-6. 欠損行の表示

```html
<!-- データのない日・週は1行で表示 -->
<div class="missing-row">
  <span class="status-dot none">●</span>
  <span class="missing-date">5/30（日）</span>
  <span class="missing-tag">― データなし</span>
</div>
```

```css
.status-dot.none { color: #aab0bb; }
.missing-tag {
  background: #f1f2ef;
  padding: 2px 9px;
  border-radius: 9px;
  font-size: 8.5pt;
  color: #9aa0ac;
}
```

### 2-7. 増減チップ（前回比・差分）

```
増加：▲ +7件 (+3.6%)    → color: #1f9d61; font-weight: 700;
減少：▼ −3件 (−30%)    → color: #d0453b; font-weight: 700;
なし：基準日 / —         → color: #9aa0ac;
```

### 2-8. 期間比較表（共通コンポーネント）

営業所の週次ビューと個人レポートの比較表で共通使用するテーブル。

```html
<table class="comparison-table">
  <thead>
    <tr>
      <th>指標</th>
      <th>今週</th>
      <th>先週</th>
      <th>差分</th>
      <th>増減率</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td class="col-label">点検件数</td>  <!-- 黒太字 -->
      <td class="num">58件</td>
      <td class="num">49件</td>
      <td class="num diff-up">+9件</td>    <!-- 増加=緑 -->
      <td class="num diff-up">+18.4%</td>
    </tr>
  </tbody>
</table>
```

```css
.comparison-table th {
  font-size: 8.5pt;
  color: #5b6270;
  text-align: right;
  border-bottom: 1px solid #cfd3d9;
  padding: 6px;
}
.comparison-table th:first-child { text-align: left; }
.comparison-table td { text-align: right; padding: 7px 6px; }
.comparison-table td:first-child {
  text-align: left;
  color: #1f2430;   /* 黒（青は使わない） */
  font-weight: 700;
}
.diff-up   { color: #1f9d61; font-weight: 700; }
.diff-down { color: #d0453b; font-weight: 700; }
.diff-zero { color: #9aa0ac; }
```

### 2-9. 表示切替（JavaScript）

```javascript
const state = { period: 'default', layout: 'list' };

function renderPanels() {
  const key = state.period + '-' + state.layout;
  document.querySelectorAll('[data-panel]').forEach(el => {
    el.hidden = el.getAttribute('data-panel') !== key;
  });
}

function bindToggle(segId, attr, stateKey) {
  const seg = document.getElementById(segId);
  if (!seg) return;
  seg.querySelectorAll('button').forEach(btn => {
    btn.addEventListener('click', () => {
      state[stateKey] = btn.getAttribute(attr);
      seg.querySelectorAll('button').forEach(b => b.classList.toggle('on', b === btn));
      renderPanels();
    });
  });
}

bindToggle('seg-period', 'data-p', 'period');
bindToggle('seg-layout', 'data-l', 'layout');
renderPanels();
```

デフォルト表示は `data-panel="[period_default]-list"` の要素を hidden なしで定義しておく。

### 2-10. 印刷（PDF）専用CSS

```css
@media print {
  /* トグルバーは紙では不要 */
  .toolbar { display: none !important; }

  /* 日次と週次は改ページで分割 */
  .print-section + .print-section { break-before: page; }

  /* ブロック途中での改ページを防ぐ */
  .metric-block, .week-block { break-inside: avoid; }

  /* 背景色・左ボーダーを印刷でも保持 */
  * {
    -webkit-print-color-adjust: exact;
    print-color-adjust: exact;
  }
}

@page {
  size: A4 portrait;
  margin: 12mm;
}
```

---

## 3. 営業所レポート 個別仕様

### 3-1. データの性質

| ビュー | 性質 | 説明 |
|---|---|---|
| 日次 | **ストック型**（累計進捗） | 月初からの累計。後の日付ほど数値が大きくなる |
| 週次 | **フロー型**（期間活動量） | その週に積み上げた量 ＋ 前週との比較 |

※ 日次の「前回比」＝ 前回データ日との差分（欠損日はスキップして直近日と比較）

### 3-2. 指標セット

| 指標 | 構成 | 備考 |
|---|---|---|
| 総合達成率 | % のみ | ブロックヘッダーに表示 |
| 点検 | 実績件 ／ 目標件 ／ 達成率% | |
| 売上 | ¥実績 ／ ¥目標 ／ 達成率% | 末見通し・対計画をサブ行で表示 |
| 次月継続 | 実績件 ／ 目標件 ／ 達成率% | |
| 保全 | 実績件 ／ 目標件 ／ 達成率% | |

### 3-3. 日次ビューのトグル構成

```
seg-period:  [日次]  [週次]    （既定: 日次）
seg-layout:  [リスト][カード]  （既定: リスト）
```

パネル ID:
- `data-panel="daily-list"`   ← デフォルト（hidden なし）
- `data-panel="daily-card"`
- `data-panel="weekly-list"`
- `data-panel="weekly-card"`

### 3-4. 達成率のしきい値

| 達成率 | 表示 |
|---|---|
| 100% 以上 | 緑 `.row-ok` |
| 100% 未満 | 赤 `.row-warn` |

※ アプリ既定のしきい値があればそちらを優先。

### 3-5. 売上行のサブ行（末見通し）

売上行の直後に字下げサブ行を挿入する。

```html
<div class="row row-warn">
  <span class="col-label">売上</span>
  <span class="col-rate warn">78%</span>
  <span class="col-detail">¥8,558,953 ／ 目標 ¥11,007,260</span>
  <span class="col-diff up">▲ +¥1,999,590 (+30.5%)</span>
</div>
<div class="sub-row">
  └ 末見通し ¥11,223,603（102%）　対計画 +¥216,343
</div>
```

### 3-6. 週次ビュー構成

```
[上部] 期間比較表（共通コンポーネント 2-8 を使用）
       列: 指標 ／ 今週 ／ 先週 ／ 差分 ／ 増減率

[下部] 週別内訳カード（各週を .week-block で囲む）
       第N週（MM/DD〜MM/DD）　X日分のデータ
       点検件数   XX件
       売上       ¥XXX,XXX
       ...
```

---

## 4. 個人レポート 個別仕様

### 4-1. データの性質

- **目標値なし**（件数・ポイントの実績のみ）
- **週次が基本単位**（「X日分のデータ」の注記付き）
- 達成率は使わない。増減（差分）で良否を表す

### 4-2. 指標セット

```
点検件数 / 促進受注額 / 促進件数 /
当月保守継続 / 次月保守継続 / 次々月保守継続 /
新規保守 / エアコン洗浄 / フルメンテリース /
営業トスアップ / アクション / 信頼関係指数（Trust pt）
```

### 4-3. トグル構成（営業所と異なる点）

日次ビューがないため、期間切替は以下に変更する。

```
seg-period:  [比較表]  [週次内訳]   （既定: 比較表）
seg-layout:  [リスト]  [カード]     （既定: リスト）
```

パネル ID:
- `data-panel="comparison-list"`   ← デフォルト
- `data-panel="comparison-card"`
- `data-panel="breakdown-list"`
- `data-panel="breakdown-card"`

### 4-4. 比較表ビュー

冒頭サマリーバンド（当期の主要合計値 ＋ 前期比）を表示したあと、共通の期間比較表コンポーネント（2-8）を使用。

```
[サマリーバンド]
  点検件数（当期）: X件   ▲ 前期比 +Y件
  促進受注額（当期）: ¥XX,XXX   ▲ 前期比 +¥Z

[期間比較表]
  列: 指標 ／ 当期 ／ 比較期 ／ 差分 ／ 増減率
```

**増減ハイライトルール（個人は達成率でなく差分で判断）：**

| 差分 | 行スタイル |
|---|---|
| プラス（増加） | `.row-ok`（緑の左ボーダー） |
| ゼロ | `.row-base`（グレーの左ボーダー） |
| マイナス（減少） | `.row-warn`（赤の左ボーダー ＋ 薄赤背景） |

ただし「ゼロが正常（例: アクション=0が良い場合）」など業務上の例外があれば、指標ごとに定義する。

### 4-5. 週次内訳ビュー

各週を `.week-block` で囲む。

```html
<div class="week-block">
  <div class="block-header">
    <span class="block-title">第2週（5/12〜5/14）</span>
    <span class="block-summary">3日分のデータ</span>
  </div>

  <!-- 指標を2列グリッドで並べる -->
  <div class="week-grid">
    <div class="wrow"><span class="col-label">点検件数</span><b>6件</b></div>
    <div class="wrow"><span class="col-label">促進受注額</span><b>¥5,100</b></div>
    ...
    <!-- Trust pt は区切りを入れて最後に -->
    <div class="wrow trust"><span class="col-label">信頼関係指数</span><b>5pt</b></div>
  </div>
</div>
```

```css
.week-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 4px 16px;
  font-size: 9.5pt;
}
.wrow {
  display: flex;
  justify-content: space-between;
  border-bottom: 1px solid #f0f1f3;
  padding: 4px 0;
}
.wrow.trust {
  grid-column: 1 / -1;           /* 2列またぎ */
  border-top: 1px solid #e3e6ea;
  margin-top: 4px;
  padding-top: 6px;
}
/* ゼロ値は薄く（ゼロでも行は表示する） */
.wrow.zero { color: #9aa0ac; }
```

### 4-6. 比較対象なし（当期が基準期の場合）

差分列・増減率列に「基準期」と表示し `.diff-zero` スタイルを適用。

---

## 5. PDF 印刷レイアウト

画面トグルが使えない印刷では、以下の順で固定出力する。

### 5-1. 営業所レポート（印刷時の構成）

```
1ページ目：日次レポート（リスト表示・全日分）
2ページ目：週次レポート（比較表 ＋ 週別内訳）
```

### 5-2. 個人レポート（印刷時の構成）

```
1ページ目：期間比較表
2ページ目：週次内訳（全週分）
```

### 5-3. 印刷時の追加ルール

- `.toolbar` → `display: none`（トグルバー非表示）
- 各ブロック（`.metric-block`, `.week-block`）→ `break-inside: avoid`
- セクション間 → `break-before: page`
- 背景色・ボーダー色保持 → `print-color-adjust: exact`
- 用紙：A4縦、余白 12mm

---

## 6. CSS クラス一覧（命名規則）

| クラス | 用途 |
|---|---|
| `.report-wrapper` | 最外側コンテナ |
| `.report-header` | タイトル ＋ 出力日 ＋ 区切り線 |
| `.summary-band` | 冒頭サマリー（背景 #f4f5f2） |
| `.toolbar` | トグルバー（印刷時 display:none） |
| `.metric-block` | 1日分 または 1週分のブロック |
| `.week-block` | 週次内訳の1週ブロック |
| `.block-header` | ブロックの見出し行 |
| `.row-ok` | 達成・増加行（緑の左ボーダー） |
| `.row-warn` | 未達・減少行（赤の左ボーダー ＋ 薄赤背景） |
| `.row-base` | 基準・ゼロ行（グレーの左ボーダー） |
| `.col-label` | 項目名（黒太字） |
| `.col-rate` | 達成率または主要値（大きく色付き） |
| `.col-detail` | 実績/目標 など補足（薄グレー） |
| `.col-diff` | 前回比・差分（右寄せ） |
| `.sub-row` | 末見通しなどの字下げサブ行 |
| `.missing-row` | データなし行 |
| `.comparison-table` | 期間比較テーブル（共通） |
| `.week-grid` | 週次内訳の2列グリッド |
| `.diff-up` | 増加値（緑太字） |
| `.diff-down` | 減少値（赤太字） |
| `.diff-zero` | 変化なし・基準期（グレー） |
| `.print-section` | 印刷時の改ページ単位 |

---

## 7. 未決事項（実装前に確認）

実装開始前に以下を確認・決定すること。

| # | 項目 | 推奨案 | 確認先 |
|---|---|---|---|
| 1 | 達成率のしきい値 | 100%未満=赤（変更なければ既定流用） | 仕様担当 |
| 2 | 個人レポートのゼロ値 | 表示する（淡色）。非表示にしたい場合は設定で切替 | 担当者 |
| 3 | 週次の集計起点 | 月曜始まり想定。月またぎは週のデータを月内分で集計 | 担当者 |
| 4 | 個人の目標値 | 現状なし → 差分で判断。将来追加時は達成率列を追加 | 担当者 |
| 5 | 増減の「良否」定義 | 基本は増加=良。アクション・フルメンテリース等は要確認 | 担当者 |

---

## 8. 実装チェックリスト

```
[ ] 項目ラベルの色を黒（#1f2430）に統一（営業所・個人 両方）
[ ] 営業所 日次ビュー（リスト）の実装
[ ] 営業所 日次ビュー（カード）の実装
[ ] 営業所 週次ビュー（比較表＋内訳）の実装
[ ] 個人 比較表ビューの実装（期間比較表コンポーネント流用）
[ ] 個人 週次内訳ビューの実装
[ ] 表示切替トグルの実装（営業所・個人 それぞれ）
[ ] 欠損日・欠損週の「データなし」行表示
[ ] 差分/増減率の色分け（増=緑・減=赤・なし=グレー）
[ ] 末見通しのサブ行表示（営業所 売上行）
[ ] Trust pt の区切り表示（個人）
[ ] @media print CSS の適用
[ ] ゼロ値の淡色表示（個人）
[ ] 7章の未決事項を確認・反映
```

---

*本仕様書は 2026-06-01 の壁打ちセッションをもとに作成。モックアップは Claude.ai 上で確認済み。*
